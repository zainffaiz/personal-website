kind = "web"
previewPath = "/"
title = "Zain Faiz - Personal Site"
version = "1.0.0"
id = "artifacts/personal-site"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25158

[services.development]
run = "pnpm --filter @workspace/personal-site run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/personal-site", "run", "build" ]
publicDir = "artifacts/personal-site/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25158"
BASE_PATH = "/"
